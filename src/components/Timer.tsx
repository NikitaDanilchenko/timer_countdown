import React, { FC } from 'react';
import { SApp, SHeader } from '../assets/styles/app.styles';

const Timer: FC = () => {
    return (
        <SApp>
            Timer
            <SHeader></SHeader>
        </SApp>
    );
};
export default Timer;
